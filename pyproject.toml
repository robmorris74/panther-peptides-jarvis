[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "peptide-operator"
version = "1.1.0"
description = "Human-governed AI operator for legitimate laboratory research materials ecommerce"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
